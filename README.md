# Algocoins Prototype

Интерактивный прототип интерфейса **Алгокоинов** с редактором миссий и витриной магазина. Основная страница `index.html` построена на Vue 2.6.14 и позволяет управлять событиями прямо в таблице без отдельных модалок.

---

## 🚀 Быстрый старт
1. Склонируйте репозиторий или скачайте файлы.
2. Откройте `index.html` в любом современном браузере — это основная страница с редактором миссий и магазином.
3. При необходимости отдельно посмотреть модальное окно можно открыть `modal.html`.

Сборка не требуется: проект написан на **HTML + CSS + JavaScript** и использует CDN-версию Vue.

---

## 📂 Структура
```
/project-root
├── index.html   # Основной интерфейс Алгокоинов на Vue 2.6.14
├── modal.html   # Изолированная демонстрация модального окна редактирования
└── images/      # Иконки для миссий и товаров
```

---

## 🧭 Описание страницы `index.html`
### Заголовок
* Название раздела и описание.
* Чекбокс «Наследовать» (демонстрационный).
* Кнопка «Добавить миссию» появляется только на вкладке редактора и создаёт новую строку для заполнения.

### Табовая навигация
* Две вкладки внутри раздела Алгокоинов: **«Редактор миссий»** и **«Магазин»**.
* Переключение вкладок производится реактивно через Vue, без перезагрузки страницы.

### Подвкладка «Редактор миссий»
* Таблица миссий с редактированием прямо в строках: иконка, название, описание, награда.
* Иконку можно заменить через input type="file"; предпросмотр обновляется сразу после выбора изображения.
* Кнопка переключателя публикации показывает состояние «Опубликовано» или «Не опубликовано» и меняет стиль строки.
* Каждая строка имеет кнопку «Сохранить» и выводит локальный статус с сообщением об успехе или ошибке под кнопкой.
* Валидация выполняется в реальном времени:
  * обязательны иконка, название и описание;
  * количество Алгокоинов должно быть числом > 0;
  * ошибки подсвечивают поле и показывают текстовое сообщение.

### Подвкладка «Магазин»
* Статическая таблица с товарами: код, цена, доступное и купленное количество.
* Кнопка «Импорт» сейчас носит демонстрационный характер.

---

## 🧑‍💻 Памятка для разработчика
* Приложение инициализируется одним Vue-инстансом внизу `index.html`.
* `data()` возвращает:
  * `tabs` — массив конфигурации для переключателя подвкладок;
  * `activeTab` — идентификатор текущей вкладки;
  * `inheritMissions` — булев флаг чекбокса;
  * `missions` — массив объектов миссий со структурой:
    ```js
    {
      id: Number,
      icon: String,          // путь или dataURL
      title: String,
      description: String,
      coins: Number,
      published: Boolean,
      errors: { title, description, coins, icon },
      feedback: { type, message },
      isNew: Boolean
    }
    ```
  * `shopItems` — список товаров магазина;
  * `nextMissionId` — счётчик для генерации новых миссий.
* Основные методы:
  * `switchTab(id)` — переключает подвкладки.
  * `addMission()` — добавляет пустую строку и показывает подсказку о необходимости сохранения.
  * `togglePublish(mission)` — меняет состояние публикации без сохранения.
  * `validateMission(mission)` — проверяет поля и заполняет `errors`.
  * `saveMission(mission)` — запускает валидацию и устанавливает `feedback` с результатом.
  * `onIconSelect(event, mission)` — считывает изображение через `FileReader` и сбрасывает ошибку иконки.
  * `setMissionFeedback(mission, type, message)` и `clearMissionFeedback` — управляют статусами строк.
* Чтобы добавить новые поля, расширьте шаблон таблицы и соответствующие объекты `missions`. Не забудьте обновить `errors` и `validateMission`.
* Вкладка магазина сейчас статична. Для интеграции с реальными данными замените `shopItems` на запрос к API и обновите шаблон при необходимости.

---

## 🖼️ Ассеты
* Иконки миссий и товары лежат в папке `images/`.
* По умолчанию используется `images/timing.jpg`, при выборе нового файла в таблице он конвертируется в `dataURL` и хранится в состоянии Vue.

---

## ✅ Возможные улучшения
* Добавить массовое сохранение и индикатор загрузки.
* Подключить бекенд для сохранения и загрузки миссий и товаров.
* Реализовать управление товарами магазина (CRUD) и импорт из файла.

